# Ports published to host:
APP_PORT=8501
API_PORT=8080

# Browser -> API URL:
NEXT_PUBLIC_API_URL=http://localhost:8080

# Data/model mount points (inside containers):
RUNS_DIR=/data/runs
WEIGHTS_DIR=/data/weights